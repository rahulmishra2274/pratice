#include<iostream>
using namespace std;
int main()
{
	long int b,d=0 ;
	int base=1,r;
	cout<<"enter the binary number";
	cin>>b;
	long int temp=b;
	while(temp>0)
	{
		r=temp%10;
		d=d+r*base;
		temp=temp/10;
		base=base*2;
	}
	cout<<"binary value"<<b;
	cout<<"decimal value"<<d;
	return 0;
}
